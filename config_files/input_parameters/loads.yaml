# load inputs
flextype: 1 # 0 if flex = number of hours delay accepted per app / 1 if first number = share of load flex, flex[1] = hours delay accepted
flex: [0.1, 5] # if first number >0 <1 : share of load flex, hours delay accepted
# 'R_eq'       : 4.2698e-07,    # value from simulink equivalent thermal resistance of the house [hr.C/J]
own_flex: 1  # 1 if all have flex, else array of ownership
own_flexP: 1  # 1 if all passive homes have flex, else array of ownership
own_loads: 1  # 1 if all have loads, else array of ownership
own_loadsP: 1
n_clus: 4
